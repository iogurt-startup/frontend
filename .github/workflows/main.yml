name: CI/CD IOUGURT FRONTEND

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-e-build-test:
    name: Linter e Build Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Instalar dependências
        run: npm ci

      - name: Rodar Linter (ESLint)
        run: npm run lint

      - name: Verificar Tipagem (Typecheck)
        run: npx tsc -b

      - name: Testar Build
        run: npm run build

  build-docker:
    name: Build e Push da Imagem Docker
    needs: lint-e-build-test
    runs-on: ubuntu-latest

    if: github.ref == 'refs/heads/main' || github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build e Push
        uses: docker/build-push-action@v5
        with:
          context: .

          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/iougurt-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  #deploy:
    #name: Deploy para o Servidor
    #needs: build-docker
    #runs-on: ubuntu-latest
    #if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    #steps:
      #- name: Deploy via SSH
        #uses: appleboy/ssh-action@v1.0.3
        #with:
          #host: ${{ secrets.SSH_HOST }}
          #username: ${{ secrets.SSH_USER }}
          #key: ${{ secrets.SSH_PRIVATE_KEY }}
          #script: |
            #cd ~/app/iougurt

            #docker compose pull frontend
            #docker compose up -d frontend